int funkcja_shortenstatic (int x) {
  if (!false && x<2) {
    printInt(1042);
  } else {
    printInt(2042);
  }

  printInt(111111);

  if (true || x<2) {
    printInt(1042);
  } else {
    printInt(2042);
  }

  return 0;
}


int main () {
  funkcja_shortenstatic(1);

  return 0;
}
